spring:
  profiles:
    active: test, admin-service, api-service
  application:
    name: Flash Sales Demo App

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration


